# DISABLED — Starlight Pages retired after Mintlify cutover (2026-07-31).
# Canonical docs: https://docs.innersync.tech (mintlify-docs).
# workflow_dispatch kept only so maintainers can rebuild a historical snapshot if needed.
name: Deploy Starlight (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Starlight Pages disabled
        run: |
          echo "docs.innersync.tech is served by Mintlify (Innersync-tech/mintlify-docs)."
          echo "Do not re-enable this workflow without updating meta/docs-sot.md."
          exit 0
