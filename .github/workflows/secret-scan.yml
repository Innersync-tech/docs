name: Secret scan

on:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  secret-scan:
    uses: Innersync-tech/Innersync-meta/.github/workflows/secret-scan.yml@cursor/secret-scan-template-ffca
    with:
      fail-on-find: false
      fetch-depth: 1
      meta-ref: cursor/secret-scan-template-ffca

