name: Secret scan

on:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  secret-scan:
    uses: Innersync-tech/Innersync-meta/.github/workflows/secret-scan.yml@main
    with:
      fail-on-find: true
      fetch-depth: 1
